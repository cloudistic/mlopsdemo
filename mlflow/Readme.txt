Installation:
	pip install mlflow
	pip install psutil -> For tracking system metrics.


Start the tracking server:
	 mlflow server -h 0.0.0.0

Access the UI using http://<SERVER IP>:5000

Start running the Python notebooks from same server

Visualize the results in experiments and models.

